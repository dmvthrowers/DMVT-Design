# CLAUDE.md — DMVT-Design

Read `AGENTS.md` first — same orientation content, tool-agnostic. This file exists only
because Claude Code auto-loads `CLAUDE.md` specifically; don't duplicate content here, it will
drift out of sync with `AGENTS.md`.

This repo is also a Claude Skill (`SKILL.md`, `/dmv-throwers-design`) — if you're here because
that skill was invoked, its own instructions (read `README.md`, then act as a design expert)
already route you correctly; `AGENTS.md` is the faster cold-orientation path if you're not
coming in through the skill invocation.
